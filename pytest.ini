[pytest]
# Ensures the project root (which contains the portfolio_tracker package)
# is on sys.path during collection, so `pytest -q` works the same whether
# invoked as a console script or via `python -m pytest`.
pythonpath = .
testpaths = tests
